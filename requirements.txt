# Wiki — Python dependencies
# Install: pip install -r requirements.txt
# Serve:   mkdocs serve
# Deploy:  mkdocs gh-deploy

# Pin mkdocs to the 1.x series.
# mkdocs 2.0 is a breaking rewrite — incompatible with mkdocs-material and all plugins.
mkdocs>=1.6.1,<2.0

# mkdocs-material 9.x — pymdown-extensions is pulled in automatically as a dependency.
mkdocs-material>=9.7.6,<10.0
